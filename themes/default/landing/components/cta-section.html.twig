<section class="cta-section bg-light">
    <div class="container text-center">
        <h2 class="mb-4">{{ 'pteroca.landing.cta.title'|trans }}</h2>
        <p class="lead mb-4">{{ 'pteroca.landing.cta.subtitle'|trans }}</p>
        <a href="{{ path('app_register') }}" class="btn btn-primary btn-lg">
            <i class="fas fa-user-plus"></i> {{ 'pteroca.landing.cta.button'|trans }}
        </a>
    </div>
</section>
