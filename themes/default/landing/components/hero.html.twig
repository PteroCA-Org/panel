<section class="hero-section">
    <div class="container">
        <div class="hero-content">
            <h1>{{ 'pteroca.landing.hero.title'|trans }}</h1>
            <p class="hero-subtitle">{{ 'pteroca.landing.hero.subtitle'|trans }}</p>
            <div class="hero-actions">
                <a href="{{ path('landing_store') }}" class="btn btn-light btn-lg">
                    <i class="fas fa-server"></i> {{ 'pteroca.landing.hero.browse_servers'|trans }}
                </a>
                <a href="{{ path('app_register') }}" class="btn btn-outline-light btn-lg">
                    <i class="fas fa-rocket"></i> {{ 'pteroca.landing.hero.get_started'|trans }}
                </a>
            </div>
        </div>
    </div>
</section>
